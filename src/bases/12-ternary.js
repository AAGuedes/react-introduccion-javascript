// Operador ternario
const enabled = true;

// let msg = '';

// if(enabled) {
//     msg = 'enabled';
// } else {
//     msg = 'disabled';
// }

// const msg = (enabled) ? 'Enabled' : 'Disabled';
// const msg = (!enabled) ? 'Enabled' : null;
const msg = enabled && 'Enabled';

console.log(msg);
